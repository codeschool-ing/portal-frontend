# bundle.py's output: generated from index.html + assets/ + app/.
# It is for emailing around or opening off disk, not for publishing — when
# served, the browser caches each file separately.
/portal-aluno.html

# The portal has no build and no dependencies: `node_modules` only exists when
# playwright is installed to run the smoke suite.
node_modules
node_modules/

# development screenshots (the `tiro-` prefix is kept: dropping the pattern
# would start committing whatever is already sitting in a working copy)
tiro-*.png
